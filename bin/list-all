#!/usr/bin/env bash

list_all_versions() {
  git ls-remote --tags --refs https://github.com/direnv/direnv.git |
    grep -o 'refs/tags/.*' |
    cut -d/ -f3- |
    grep stable |
    sort -V
}

list_all_versions | xargs echo
